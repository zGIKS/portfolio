import { HomeCard } from "@/components/home-card";

export default function Home() {
  return (
    <main className="flex min-h-screen justify-center bg-neutral-100 px-[clamp(2rem,1.0816rem+3.9184vw,5rem)]">
      <HomeCard />
    </main>
  );
}
